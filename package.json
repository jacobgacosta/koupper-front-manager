{
  "name": "koupper-front-manager",
  "version": "1.0.0",
  "description": "A package to configure the UI dependencies inside a project",
  "main": "app.js",
  "scripts": {
    "test": "cross-env NODE_ENV=production webpack --config webpack.config.js --watch --colors"
  },
  "author": "J.G Acosta",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
